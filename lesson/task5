#НАПИШИТЕ ПРОГРАММУ, КОТОРАЯ ПЕРЕБИРАЕТ ЧИСЛА ОТ 1 ДО 10 И ЗАПИСЫВАЕТ ТОЛЬКО ЧЕТНЫЕ.

for i in range(1, 11):
    if i % 2 == 0: # два знака равно обозначает в питоне, что мы проверяем, является ли наше значение равным какому-то другому(в данном случае у нас 0)! один знак = - присваивание
        print(i)
